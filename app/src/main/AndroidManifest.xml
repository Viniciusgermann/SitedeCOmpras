<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sitedecompras">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ListasAbertasActivity" />
        <activity android:name=".ListasFechadasActivity" />
        <activity android:name=".ListaAtualActivity" />
        <activity android:name=".EstabelecimentosActivity" />
        <activity android:name=".ComparadorDePrecosActivity" />
        <activity android:name=".ConfiguracoesActivity" />
        <activity android:name=".DetalhesProdutoActivity" />
        <activity android:name=".EditarProdutoActivity" />

    </application>

</manifest>
